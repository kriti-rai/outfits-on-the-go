<br>
<h5><em><font color="grey"><%= @outfit.caption %> </em></h5>

<div><%= image_tag @outfit.image.url, {class: "outfit-show"}%></div>

<p><%= link_to "Go Back", @outfit.board %> | <%= link_to "Edit Caption", edit_outfit_path(@outfit) %> | <%= link_to "Delete Outfit", @outfit, method: :delete, data: {confirm: "Are you sure?"}%></p>
